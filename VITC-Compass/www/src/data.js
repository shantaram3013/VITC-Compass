App.content = {
    "Home": {
        "text": "Open the menu in the top left to get started!"
    },
    "Places of Interest": {
        "text": `Notice we set the background-size to the size of the logo element. We have to do that otherwise
        we’ll just see a bit of the upper left of our much larger original SVG image. These numbers are aspect-ratio
        aware of the original size. But you could use a background-size keywords like contain if you want to make 
        sure the image will fit and can’t know the parent image will be of the exact right size.`
    }
}